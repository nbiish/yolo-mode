{
  "commands": [
    {
      "name": "yolo",
      "description": "Start YOLO Mode - an autonomous agent loop that takes a prompt, plans tasks, and iteratively executes them",
      "command": "python3",
      "args": ["yolo_mode/scripts/yolo_loop.py"],
      "arguments": [
        {
          "name": "prompt",
          "description": "The main goal/prompt for YOLO Mode to achieve",
          "required": true
        }
      ]
    },
    {
      "name": "yolo-tts",
      "description": "Start YOLO Mode with TTS output enabled",
      "command": "python3",
      "args": ["yolo_mode/scripts/yolo_loop.py", "--tts"],
      "arguments": [
        {
          "name": "prompt",
          "description": "The main goal/prompt for YOLO Mode to achieve",
          "required": true
        }
      ]
    }
  ]
}
