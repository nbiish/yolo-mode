---
description: "Start YOLO Mode in forever mode (continuous autonomous loop)"
allowed-tools: ["Bash", "Read", "Write", "Edit", "Glob", "Grep", "WebFetch", "WebSearch", "Task"]
---

# YOLO Mode (Forever)

Initialize autonomous YOLO mode in **Forever Mode** for goal: $ARGUMENTS

Max iterations set to 1000. The loop will continue autonomously.

The Ralph Loop hook will take over and autonomously execute tasks from `YOLO_PLAN.md`.
